---
"echadron": patch
---

Wrap the goal panel's "no stop condition" line instead of truncating it. Every other row in that panel wraps to the panel width; this one did not, so a narrow terminal cut the sentence off mid-word.
