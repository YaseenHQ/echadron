---
"echadron": minor
---

Add a `--thinking` flag for session-scoped effort overrides, including resumed sessions. Pass `--thinking max` to use maximum effort without changing the `config.toml` default.
