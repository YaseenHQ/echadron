---
"echadron": patch
---

Refresh existing models.dev-backed providers on startup and with `echadron update --models`. Newly published models and catalog metadata now appear without re-login or replacing the provider's saved credentials, default model, or explicit model overrides.
